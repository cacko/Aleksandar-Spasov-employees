
### 🖥️ CLI (Command-Line Interface)

   Save the code above as find_pairs.php.

   Save your sample data as employee_projects.csv in the same directory.

   Open your terminal or command prompt.

   Run the script by passing the CSV file as an argument:

   ```bash
      php find_pairs.php employee_projects.csv
   ```

### 🌐 Simple UI (Web Browser)

   Save the code above as find_pairs.php.

   Place this file in a directory served by a local web server (e.g., XAMPP, MAMP, WAMP, or using PHP's built-in server ```php -S localhost:8000```).

   Open your web browser and navigate to the script (e.g., http://localhost/find_pairs.php or http://localhost:8000/find_pairs.php).

   You will see the upload form.

   Click "Choose File", select your employee_projects.csv file, and click "Analyze".

   The page will reload and display the result.
